<Page x:Class="CharTracker.View.LogInPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CharTracker.View"
      xmlns:core="clr-namespace:CharTracker.Core"
      mc:Ignorable="d" 
      DataContext="{Binding Terminal, Source={StaticResource Locator}}">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" DataContext="{Binding Main}">
        <TextBlock Text="We need for you to login with your Google Account!" Style="{StaticResource StandardText}" Margin="0,0,0,8" />
        <Border CornerRadius="8" Padding="4" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{StaticResource Rot}" Width="126" Height="40" core:MouseBehaviour.MouseUpCommand="{Binding LogInCommand}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Log In" Style="{StaticResource StandardText}" />
            </StackPanel>
        </Border>
    </StackPanel>
</Page>
