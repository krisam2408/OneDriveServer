<Page x:Class="CharTracker.View.LogInPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CharTracker.View"
      xmlns:core="clr-namespace:CharTracker.Core"
      xmlns:controls="clr-namespace:CharTracker.View.UserControls"
      mc:Ignorable="d">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="We need for you to login with your Google Account!" Style="{StaticResource StandardText}" Margin="0,0,0,8" />
        <controls:ButtonControl BorderBackground="{StaticResource Rot}" Text="Log In" Command="{Binding Terminal.Main.LogInCommand, Source={StaticResource Locator}}" IsEnabled="{Binding Terminal.Navigation.IsEnabled, Source={StaticResource Locator}}" BorderWidth="126" BorderHeight="40" />
    </StackPanel>
</Page>
